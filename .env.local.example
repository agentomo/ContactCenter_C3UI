# Copy this file to .env.local and fill in your Genesys Cloud OAuth credentials.
# These variables are used for local development. For deployment, you must set
# these as environment variables in your hosting provider (e.g., Firebase App Hosting).

# The Client ID of your OAuth Client in Genesys Cloud.
GENESYS_CLIENT_ID=""

# The Client Secret of your OAuth Client. This is a secret and should not be committed.
GENESYS_CLIENT_SECRET=""

# The region your Genesys Cloud organization is in (e.g., us-east-1, mypurecloud.de).
GENESYS_REGION=""

# The redirect URI configured in your Genesys Cloud OAuth client.
# This should be the full URL to this application's `/login/callback` page.
# For local development, this is typically http://localhost:9002/login/callback
GENESYS_REDIRECT_URI="http://localhost:9002/login/callback"
